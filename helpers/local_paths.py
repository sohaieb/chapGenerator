from os import path
import sys

project_path = path.join(sys.path[0])
ressources_path = path.join(project_path, 'assets')
output_path = path.join(project_path, 'output')
default_exec_path = path.join(project_path, 'bin')
